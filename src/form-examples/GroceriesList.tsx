import { cn } from '@/lib/utils';

export const GroceriesListFormExample: React.FC<{ className?: string }> = (props) => {
  return <div className={cn(props.className)}>
    Groceries list form example (TODO)
  </div>
}
